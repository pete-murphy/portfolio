{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files \"*.js, *.css, *.html, ./**/*.js, ./**/*.css, ./**/*.html\" --directory",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Murphy",
  "license": "MIT",
  "dependencies": {
    "node-sass": "^7.0.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "tape": "^4.9.2"
  }
}
